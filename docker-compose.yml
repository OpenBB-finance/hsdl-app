services:
  api:
    build: .
    command: serve
    ports:
      - "${PORT:-7780}:7780"
    environment:
      HSDL_DB_PATH: /app/data/hsdl_catalog.db
      HSDL_DATA_DIR: /app/data
    volumes:
      - hsdl_data:/app/data

volumes:
  hsdl_data:
